<div class="{{ craft.wabiClasses.components('inner', options) }}">
    {% block content %}
    <div>
        {{ content|raw }}
    </div>
    {% endblock %}
    {% block button %}
        {{ wabiComponents({
            path: 'notice/button.twig',
            options: options.button|default(null)
        }) }}
    {% endblock %}
</div>
